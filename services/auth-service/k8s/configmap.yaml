apiVersion: v1
kind: ConfigMap
metadata:
  name: auth-service-config
  namespace: auth-service
data:
  DEBUG: "False"
  ALLOWED_HOSTS: "*"
  DB_NAME: "auth_db"
  DB_USER: "postgres"
  DB_HOST: "postgres-svc"
  DB_PORT: "5432"
